# ----------------------------------------------------------------------------
# Airflow Image Configuration
# ----------------------------------------------------------------------------
AIRFLOW_IMAGE_NAME=apache/airflow:2.10.5
AIRFLOW_UID=50000
AIRFLOW_PROJ_DIR=.

# ----------------------------------------------------------------------------
# Airflow UI Configuration
# ----------------------------------------------------------------------------
# Set to False to disable example DAGs in Airflow UI (keeps UI clean)
AIRFLOW__CORE__LOAD_EXAMPLES=False

# ----------------------------------------------------------------------------
# Helical Model Container - Local File System Paths
# ----------------------------------------------------------------------------

HELICAL_DATA_PATH=/path/to/helical-assignment/helical-model/data
HELICAL_OUTPUT_PATH=/path/to/helical-assignment/helical-model/outputs
HELICAL_SCRIPTS_PATH=/path/to/helical-assignment/helical-model/scripts

# Note: Update the above paths to match your local file system structure
# where the Helical model's data, outputs, and scripts are stored.